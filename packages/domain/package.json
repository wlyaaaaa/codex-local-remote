{
  "name": "@codex-local-remote/domain",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@codex-local-remote/contracts": "workspace:*",
    "@codex-local-remote/security": "workspace:*"
  }
}
